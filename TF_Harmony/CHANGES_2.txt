CHANGES_2.txt - Session 2 (April 2026)

OUTSTANDING ITEMS (from Kirk's email)
- Tanglegram MSA: blocked on Kirk/Tim providing a better MSA file.
- Cell Type Specificity column order: back to default for now, needs
  Kirk/Tim input on a fixed biological tissue ordering.
- Tim's multiple motifs per TF: blocked on new motif data files.
- Leiden modules: now the ONLY algorithm. Louvain removed entirely per Kirk.
  Uses igraph::cluster_leiden with objective_function = "modularity".
  Resolution parameter slider could be added later if needed.
- Motif heatmap tick colors: working but imperfect (drift on zoom).
- Motif dendrogram: colored labels but lost interactivity.
- qs2 serialization: Kirk suggested replacing RDS with qs2 for faster
  load times. Easy to test, low priority.
- Harmony table parquet: Kirk suggested parquet for partial loading.
  Low priority, loading is acceptable.
- GO terms local computation: Kirk prefers GOST server results. Optional.
- User data upload and harmony recalculation: BOTH IMPLEMENTED.
  1-col + 2-col upload to Target Regulation; 2-col enables per-TF Pearson
  similarity (scatter labels, tooltips, ranking table).
  Recalculated harmony uses subset-proportion NP (Kirk's preference),
  auto-switches on upload, manual toggle available.
  Fisher background = genome size (32031) — open question, may revisit.

---

Global - Family Harmony tab
- Removed entirely. Kirk didn't find it useful.
- Removed tabPanel from ui.R
- Removed output$relationships (renderPlotly) and output$familyproportion
  (renderVisNetwork) from server
- subdt() reactive is still used by other tabs (heatmaps, tanglegram, clustering)

Target Regulation
- Integrated Kirk's March 24 rewrite: scatter plot, Louvain modules, heatmap, new network
- Removed old regulators network, tfregdt, targetregdt, subvs(), targetnetworkstyle
- Added compute_louvain_modules() to helpers.R
- Added signed Harmony column to preprocess_data.R
- Added library(ggrepel)
- Fixed duplicate node IDs crash when a target is also a TF
- Removed bottom heatmap (targetRegHeatmap output + heatmap_dt reactive).
  Kirk didn't want it after seeing it in context.

Pairwise - TF x TF
- Table width 50% -> 30%

Pairwise - Motif Comparison
- Removed tree style dropdown, default to "tree"
- Added motif similarity table (ALLR_LL) beside the tree
- Increased motif tree SVG height
- Tim's multiple motifs per TF idea: current data only has _up and _down per TF.
  Needs new motif data files to implement. Once added, can add a top-N dropdown.

Pairwise - Cell Type Specificity
- Reverted: column (tissue) clustering removed. Columns are back to default order.
  Tried clustering tissues by expression similarity, Kirk preferred the original
  ordering. Fixed biological tissue order (e.g. radial root layers) still TBD —
  needs Kirk/Tim input.

Pairwise - NxTime Overlay
- Reverted: rows are back to JIT bin ordering (both root and shoot).
  Tried p-value ordering, Kirk preferred the original JIT ordering.

Pairwise - GO Terms
- Removed separate goplotheight slider from GO Terms subtab.
  GO plot now uses the shared harmonyplotheight slider from the top input panel.

Pairwise - TF Regulation
- Rewrote to use visNetwork directly instead of visIgraph/tbl_graph.
- TFs shown as module-colored triangles with synonym labels.
- Louvain module detection on harmony between selected TFs.
- Concordant edges red (solid), discordant edges blue (dashed).
- Removed network style dropdown and DEGs-only checkbox.

Pairwise - DEG Networks
- Rewrote to use visNetwork directly instead of visIgraph with vertices.
- TFs as module-colored triangles, targets as boxes.
- Louvain module detection on selected TFs via harmony.
- Edge width by log2FC, color by regulation direction.
- Default cutoffs raised: log2FC 0->2, degree 0->2 to fix severe
  performance issues. Previously loaded thousands of nodes/edges.
- Removed network style dropdown.

Dead code removed
- testcolors, vertices, ids, allteds from data_loading.R
- network_layouts from ui.R
- library(ggraph) and library(tidygraph) — no longer used anywhere.

Leiden module detection (Louvain removed)
- Module detection active in all three network tabs (Target Regulation,
  TF Regulation, DEG Networks). All use compute_modules() in helpers.R.
- Switched from Louvain to Leiden via igraph::cluster_leiden (igraph 2.3.0+).
  NO Python/leidenAlg dependency — that note in CHANGES was wrong.
- objective_function = "modularity", n_iterations = 2.
- Louvain removed entirely from the app per Kirk — no toggle, no fallback,
  no backwards-compat wrapper. compute_louvain_modules() removed.
- input$louvain_weight_mode renamed to input$module_weight_mode for clarity
  (was always algorithm-agnostic, just badly named).
- Resolution-parameter slider deferred for now.

Global - Motif Heatmap
- Axis labels colored red (up) / blue (down) via HTML spans in plotly ticktext.
  Keeps interactivity (hover/zoom). Workaround for plotly not supporting
  per-tick colors natively — not perfect, tick alignment can drift on zoom.

Global - Motif Dendrogram
- Switched from ggplotly to static plot with dendextend for label coloring.
  Labels colored red (up) / blue (down). Lost interactivity — plotly strips
  dendextend colors. Kirk had the same issue previously.

Tanglegram
- No code changes. Blocked on Kirk/Tim providing a better MSA file to
  replace the current msa.fa (low quality, few bootstraps). Phytozome
  or Tim's MSA are options. Once replaced, just rerun preprocess_data.R.
